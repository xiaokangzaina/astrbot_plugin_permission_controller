# 更新日志

## v1.5.1

- 修复图片审核传入非公网 URL、本地文件或 `base64://` 时，OpenAI 兼容接口报 `image_url invalid format` 的问题。
- 图片审核现在会优先使用有效公网 URL，否则自动转换为 `data:image/jpeg;base64,...`。
- 兼容部分中转接口对 `image_url` 字段格式的差异，必要时自动改用字符串格式重试。
- 优化配置页混色主题样式和右侧配置区滚动体验。

## v1.5.0 重大UI更新

- 更换插件头像/logo 为新的“插电”头像。
- 自带前端升级为控制台风格 UI。
- 审核群组列表顶部新增群配置、已启用、未启用统计。
- 修复审核群组搜索框图标错位和底部大面积空白问题。
- 修复页面主题偏好未持久化的问题，新增后端主题保存接口。
- 右侧配置栏加入蓝紫青粉渐变混色渲染，并优化浅色/深色主题观感。
- 修复右侧配置页面折叠卡住的问题，恢复稳定滚动和卡片布局。

## v1.4.26

- 优化处罚结果文案：禁言和踢人都未开启时，显示“未开启禁言/踢出”，不再显示“未达到禁言/踢出阈值”。
- 禁言是否启用由“单人违规阈值 > 0 且禁言时长 > 0”决定。
- 踢人相关判断统一受“是否启用踢人”控制；未启用踢人时不再按禁言次数踢出。

## v1.4.25

- 修复禁言时长为 0 时仍调用 set_group_ban(duration=0)，导致机器人误解除违规人禁言的问题。
- 禁言时长 <= 0 时不再触发禁言，只撤回违规消息并发送通知。
- `_mute_user` 增加二次保护，彻底避免 duration=0 被发送到 OneBot。
- 包含 v1.4.24：未启用踢人时隐藏“违规次数/被踢阈值”相关通知行。
- 清理明确无用代码：移除未使用的 asyncio 导入、函数内重复 json 导入和 Python 编译缓存。

## v1.4.24

- 优化确认违规通知：配置页未启用踢人时，自动隐藏“违规次数/被踢阈值”相关行。
- 即使自定义通知模板里包含“违规次数”或“被踢阈值”，未启用踢人时也会自动过滤。
- 配置提示中补充该隐藏规则说明。

## v1.4.23

- 将群配置里的“时间窗口”单位改为天数。
- 新保存的时间窗口会写入单位标记，超大天数保存刷新后不再被误当成旧秒值换算。
- 兼容旧版本秒值配置，以及 v1.4.21/v1.4.22 的小时配置。

## v1.4.22

- 修复 OpenAI 兼容审核 API 偶发调用异常后需要重启插件才能恢复的问题。
- HTTP 客户端连接失效、连接池异常或网络异常时，会自动关闭旧客户端、重建并重试一次。
- 5xx 服务端/网关错误会重建客户端后重试一次；4xx 配置/鉴权错误不盲目重试。
- 修复插件内重复 terminate 定义导致卸载时 HTTP 客户端可能未正确关闭的问题。
- 包含 v1.4.21：时间窗口单位从秒改为小时，并兼容旧秒值配置。

## v1.4.21

- 将群配置里的“时间窗口”单位从秒改为小时。
- 后端处罚统计会把小时自动换算为秒参与判断。
- 兼容旧版本秒值配置，避免旧配置直接变成超长小时窗口。
- 前端群配置展示旧秒值时会自动换算为小时。

## v1.4.20

- 前端群配置新增“确认违规通知模板”和“疑似违规通知模板”。
- 支持用中文说明的占位符自定义通知内容。
- 确认违规模板支持：{at}、{type}、{reason}、{penalty}、{violations}、{kick_threshold}。
- 疑似违规模板支持：{at}、{type}、{reason}。
- 自定义模板发送到通知群时不再额外追加群号和用户信息。

## v1.4.19

- 精简疑似违规通知文案。
- 疑似违规通知改为艾特发送者并只显示原因。
- 避免疑似违规转发到通知群时额外追加群号和用户信息。

## v1.4.18

- 精简确认违规后的群通知文案。
- 移除“检测到违规内容、群成员、类型、原因”等独立行，改为“你因类型违规：原因”。
- 违规次数和被踢阈值合并到同一行展示。

## v1.4.17

- 违规次数、群违规次数和禁言次数改为本地持久化保存，插件重载后不再清零。
- 违规记录永久保留，直到手动清除；惩罚判断仍按配置的统计时间窗口计算。
- 群配置底部新增“清除违规次数”按钮，可清除当前群的违规/禁言计数记录。

## v1.4.16

- 移除顶部“保存插件配置”按钮。
- 底部“保存此群配置”现在会同时保存日志级别和当前群审核处置配置。
- 更新插件配置区提示文案，避免两个保存入口造成混淆。

## v1.4.15

- 移除自带前端中重复手写的“是否启用审核”开关。
- 仅保留配置 schema 原生的 enabled 字段，避免和文本审核开关视觉混淆。

## v1.4.14

- 修复自带前端删除群配置后重新进入仍显示的问题。
- 后端删除时统一按字符串群号比较，确保同步删除插件主页配置里的 disposal.group_custom。
- 删除接口返回最新群配置列表，前端删除后立即清空当前项并同步列表。

## v1.4.13

- 自带前端 UI 重排为两列表单卡片布局。
- 按常用逻辑重新排序群审核处置配置字段。
- 主题色改为粉色，优化浅色/深色主题下的卡片、按钮和选中态。
- 大文本字段跨两列显示，审核提示词输入区域更清晰。

## v1.4.12

- 自带前端“插件配置”仅保留日志级别。
- 日志级别改为下拉选项：DEBUG / INFO / WARNING / ERROR。
- 移除自带前端插件配置区中的 AI 审核接口、模型、超时和全局审核提示词展示。
- 群审核处置配置完全保留。

## v1.4.11

- 自带前端新增“插件配置”配置表，支持像普通插件配置页一样编辑全局配置。
- 新增独立保存插件配置按钮和 `/settings/global` 保存接口。
- 群配置表恢复按默认群模板渲染完整字段，审核提示词继续支持多行文本框。

## v1.4.10

- 精确修复 AstrBot 原生插件配置页：审核处置配置 → 群级别自定义配置 → 默认群配置 → 审核提示词。
- 将该字段类型由 string 改为 text，确保原生配置页显示为可换行文本框。

## v1.4.9

- 审核提示词配置改为多行文本域，支持换行填写。
- 加高全局审核提示词和分群审核提示词输入框。

## v1.4.8

- 原生配置页面分群模板支持 `display_item=remark_name`。
- 分群配置展开标题可直接显示备注名。

## v1.4.7

- 配置表单展开项标题优先显示备注名。

## v1.4.6

- 备注名用于替代模板标签，不替代真实群名。

## v1.4.5

- 分群审核模板只保留默认群配置。
- 移除严格配置和宽松配置。
- 默认群配置新增备注名字段。

## v1.4.4

- 简化疑似违规通知格式，只保留标题、原因和用户。

## v1.4.3

- 达到最终踢出阈值时直接踢出，不再先禁言。

## v1.4.2

- 通知内容优化。
- 违规处理逻辑优化。

## v1.4.1

- 增加群级审核提示词。
- 支持文本和图片不同审核策略。

## v1.4.0

- 支持 OpenAI 兼容审核接口。
- 支持文本/图片审核、撤回、禁言、踢出、通知群提醒。

## 维护者

xiaokangzaina
